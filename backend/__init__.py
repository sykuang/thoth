"""Backend package: Python crawlers + DB + core framework.

Structure:
    backend/banks/   — bank-specific crawlers (cathay, ubot, hsbc, ctbc, sinopac, scsb)
    backend/core/    — framework (base, captcha, creds, store, persist)
    backend/data/    — SQLite DBs + collected JSON snapshots + browser sessions
"""
